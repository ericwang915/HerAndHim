# HerAndHim — single-tenant self-hosted configuration
# Copy to .env, fill in the required keys, then: docker compose up
# Anything left unset degrades gracefully (that feature just turns off).
#
# These are applied on top of /data/herandhim.json on every boot. Anything
# you set here wins; anything you leave unset keeps whatever the wizard or
# the dashboard saved. A restart never wipes the file.

# ── Required: one text-LLM key ────────────────────────────────────────
# Set the key for ONE provider — it's auto-detected, no need to also set
# HERANDHIM_LLM_PROVIDER. Uncomment whichever you have.
#
# OpenRouter — one key, hundreds of models, easiest to start  https://openrouter.ai
HERANDHIM_OPENROUTER_API_KEY=sk-or-your-key-here
# HERANDHIM_OPENAI_API_KEY=sk-...                     https://platform.openai.com
# HERANDHIM_DEEPSEEK_API_KEY=sk-...                   https://platform.deepseek.com
# HERANDHIM_CLAUDE_API_KEY=sk-ant-...                 https://console.anthropic.com
# HERANDHIM_GEMINI_API_KEY=...                        https://aistudio.google.com
# HERANDHIM_GROK_API_KEY=xai-...                      https://console.x.ai
# HERANDHIM_QWEN_API_KEY=sk-...                       https://dashscope.aliyun.com
# HERANDHIM_GROQ_API_KEY=gsk_...                      https://console.groq.com
# HERANDHIM_MISTRAL_API_KEY=...                       https://console.mistral.ai
# HERANDHIM_TOGETHER_API_KEY=...                      https://api.together.ai
# HERANDHIM_SILICONFLOW_API_KEY=sk-...                https://siliconflow.cn
# HERANDHIM_KIMI_API_KEY=sk-...  ·  HERANDHIM_GLM_API_KEY=...
#
# Fully local, no key, nothing leaves your machine (run Ollama on the host):
# HERANDHIM_LLM_PROVIDER=ollama
# HERANDHIM_OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
# HERANDHIM_OLLAMA_MODEL=llama3.1
#
# Override the model for any provider: HERANDHIM_<PROVIDER>_MODEL=...

# Your Telegram bot, created via @BotFather (/newbot → copy the token).
HERANDHIM_TELEGRAM_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# Your own numeric Telegram user id (ask @userinfobot). Keeps strangers out
# of your bot — and out of your API budget.
HERANDHIM_TELEGRAM_ALLOWED_USERS=12345678

# ── Optional — photos ─────────────────────────────────────────────────
# Selfies and candid shots. Set ONE of these — the backend is auto-detected,
# same as the LLM. Skip them all and everything else still works.
#
# Zero signup:
#   Nothing to set. Uses a free public service (your prompts go to it).
# HERANDHIM_IMAGE_PROVIDER=pollinations
#
# Keys you may already have — no second signup:
#   Gemini: the key below also gives her vision, so one key does both.
# HERANDHIM_IMAGE_GEMINI_KEY=                         https://aistudio.google.com
#   OpenRouter: the same key from the quickstart also generates images.
# HERANDHIM_IMAGE_OPENROUTER_KEY=                     https://openrouter.ai/keys
#   Qwen / Wan, via DashScope — reuses an Alibaba account.
# HERANDHIM_DASHSCOPE_API_KEY=          https://dashscope.console.aliyun.com
#
# Best face consistency (she stays recognisably the same person):
#   Black Forest Labs — FLUX.1 Kontext preserves a reference subject.
# HERANDHIM_BFL_API_KEY=                                     https://api.bfl.ai
#   Seedream (BytePlus ModelArk) — ~$0.035/image.
# HERANDHIM_SEEDREAM_API_KEY=      https://www.byteplus.com/en/product/modelark
#   OpenAI gpt-image-1.
# HERANDHIM_IMAGE_OPENAI_KEY=                       https://platform.openai.com
#
# Other hosted:
# HERANDHIM_FAL_KEY=                                            https://fal.ai
# HERANDHIM_REPLICATE_API_TOKEN=                          https://replicate.com
# HERANDHIM_STABILITY_API_KEY=                       https://platform.stability.ai
#
# Fully local — no key, no upload, nothing about her appearance leaves
# your machine. Point at whichever you already run:
# HERANDHIM_COMFYUI_BASE_URL=http://localhost:8188
# HERANDHIM_SDWEBUI_BASE_URL=http://localhost:7860
#
# Anything else speaking the OpenAI image API (Together, DeepInfra, Novita,
# SiliconFlow, Fireworks…):
# HERANDHIM_IMAGE_PROVIDER=custom
# HERANDHIM_IMAGE_API_KEY=
# HERANDHIM_CUSTOM_BASE_URL=
#
# Pin a specific model on whichever backend you chose:
# HERANDHIM_IMAGE_MODEL=

# ── Optional — seeing your photos (vision) ───────────────────────────
# If your chat model can't take images, name a second model for the turns
# that carry one. Any provider works; endpoint and key default to that
# provider's own settings, so provider + model is usually enough.
#
# Fully local: llama3.1 for chat, llava (or qwen2.5vl, moondream, minicpm-v)
# for photos — both on the Ollama you already run:
# HERANDHIM_VISION_PROVIDER=ollama
# HERANDHIM_VISION_MODEL=llava
#
# Or point it at a different endpoint / key than the chat model uses:
# HERANDHIM_VISION_BASE_URL=http://gpu-box:11434/v1
# HERANDHIM_VISION_API_KEY=
#
# Shortcut: with nothing above set, a Gemini key alone also gives her vision.
# HERANDHIM_GEMINI_API_KEY=

# ── Optional — voice & search ─────────────────────────────────────────
# Deepgram — she understands your Telegram voice notes. https://deepgram.com
# HERANDHIM_DEEPGRAM_API_KEY=
# Tavily — live web search grounding. https://tavily.com
# HERANDHIM_TAVILY_API_KEY=

# ── Optional — dashboard access ──────────────────────────────────────
# The dashboard has no login: it assumes it's yours, on your machine.
# Do NOT expose port 7788 to the public internet — put it behind a VPN,
# an SSH tunnel, or a reverse proxy that does the authenticating.
#
# Only needed if you serve the dashboard from a different origin:
# HERANDHIM_ALLOWED_ORIGINS=https://dash.example.com

# ── Optional — self-imposed limits ───────────────────────────────────
# All off by default: it's your machine and your API bill. Set these only
# if you want a guard rail (a small VPS, or a bot several people use).
# Configure under "quota" in herandhim.json:
#   diskMb         cap the data directory   (0 = unlimited)
#   dailyMessages  cap chat turns per day   (0 = unlimited)
#   dailyPhotos    cap generated photos/day (0 = unlimited)

# ── Paths / ports ─────────────────────────────────────────────────────
HERANDHIM_HOME=/data
PORT=7788
