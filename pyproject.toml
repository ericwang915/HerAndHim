[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "herandhim"
version = "0.2.0"
description = "HerAndHim — your virtual AI partner (boyfriend or girlfriend) on Telegram, powered by LLM with memory, RAG, and skills."
readme = "README.md"
license = {text = "AGPL-3.0-only"}
requires-python = ">=3.10"
authors = [
    {name = "Eric Wang", email = "wangchen2007915@gmail.com"},
]
keywords = ["llm", "agent", "ai", "telegram", "chatbot", "ai-companion", "virtual-girlfriend", "virtual-boyfriend", "memory", "rag"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
]
dependencies = [
    "openai",
    "anthropic",
    "google-genai>=1.0",
    "python-telegram-bot[job-queue]>=20.0",

    "apscheduler>=3.10",
    "pyyaml>=6.0",
    "fastapi>=0.100",
    "uvicorn[standard]>=0.20",
    "httpx>=0.24",
    "tavily-python",
    "rank-bm25>=0.2",
    "numpy>=1.24",
    "beautifulsoup4",
    "requests",
]

# Optional feature sets.
#   pip install -e ".[search]"   # TF-IDF recall — better memory search, but
#                                #   pulls in scipy (~130 MB). Without it
#                                #   retrieval falls back to character-bigram
#                                #   similarity, which still works.
#   pip install -e ".[twitter]"  # the Twitter/X skill
#   pip install -e ".[all]"      # everything (what the Docker image installs)
[project.optional-dependencies]
search = ["scikit-learn>=1.3"]
twitter = ["tweepy>=4.14"]
all = ["scikit-learn>=1.3", "tweepy>=4.14"]

[project.scripts]
herandhim = "herandhim.main:main"

[project.urls]
Homepage = "https://github.com/ericwang915/HerAndHim"
Documentation = "https://github.com/ericwang915/HerAndHim#readme"
Repository = "https://github.com/ericwang915/HerAndHim"
Issues = "https://github.com/ericwang915/HerAndHim/issues"

[tool.setuptools.packages.find]
include = ["herandhim*"]

[tool.setuptools.package-data]
herandhim = [
    "templates/**/*.py",
    "templates/**/*.sh",
    "templates/**/*.yaml",
    "templates/**/*.md",
    "templates/**/*.json",
    "web/static/**/*",
]

[tool.setuptools.exclude-package-data]
"*" = ["__pycache__", "*.pyc", "*.pyo"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
